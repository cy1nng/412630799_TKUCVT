# 期中實作 — <412630799> <陳羿陵>

## 1. 架構與 IP 表
<Mermaid 圖 + 表格>

## 2. Part A：VM 與網路
<命令 + 關鍵輸出>
命令：hostname -I (在兩台 VM 執行)
關鍵輸出：
dev-a: 192.168.64.3 192.168.128.3
server-b: 192.168.64.100

## 3. Part B：金鑰、ufw、ProxyJump
<防火牆規則表 + ssh app 成功證據>

## 4. Part C：Docker 服務
<systemctl status docker + curl 輸出>

## 5. Part D：故障演練
### 故障 1：<F1/F2/F3 擇一>
- 注入方式：
- 故障前：網路未初始化。
- 故障中：ssh-copy-id 噴出 Connection refused
- 回復後：使用 ip addr add 192.168.64.100/24 重新分配唯一位址
- 診斷推論：封包因目標 IP 與來源 IP 相同，引發 Loopback 效應，導致連線未出網卡即被拒絕

### 故障 2：<另一個>
（同上）
注入方式：sudo ufw deny 22/tcp 或 sudo ufw enable 但未開放 Port 22
故障前：SSH 連線正常
故障中：連線顯示 Connection timed out
回復後：sudo ufw allow 22/tcp
診斷推論：防火牆策略 (Policy) 將封包丟棄 (DROP)，導致發起端持續等待回應直到逾時

### 症狀辨識（若選 F1+F2 必答）
兩個都 timeout，我怎麼分？

## 6. 反思（200 字）
這次做完，對「分層隔離」或「timeout 不等於壞了」的理解有什麼改變？
這次實作讓我對「分層隔離」有了全新的認識。最初以為只要服務開啟就能連通，沒想到在虛擬化環境中，連最基礎的 IP 分配都會出現衝突。
這讓我理解到，故障排查不能只看應用層（SSH），必須從 L3 網路層開始診斷。「Timeout 不等於壞了」是我最大的領悟。
有時候服務跑得很完美，但僅僅是因為防火牆一個 DROP 規則，就能讓外部看起來像斷線一樣。在分層架構中，每一層的「安靜」都有不同的技術意涵，
學會解讀這些「沉默的錯誤」，才是系統運維的核心價值。
## 7. Bonus（選做）
```

## 評分（100 分 + 最多 15 分加分）

| 項目                                  | 分數 |
| ------------------------------------- | ---: |
| Part A 網路配置正確                   |   20 |
| Part B 金鑰 + ufw + ProxyJump         |   25 |
| Part C Docker 服務可達                |   15 |
| Part D 兩次故障三階段完整性與診斷推理 |   25 |
| README 結構與反思                     |   15 |
| **Bonus 1** Dockerfile 優化     |  +10 |
| **Bonus 2** Cgroup 限制觀察     |   +5 |

## Bonus 1｜Dockerfile 優化
